# logger ✍️
Kotlin logger. It can work with Flow

### You can find a usage example in the file `example.kt`

### The library allows you to work with custom loggers.
To do it, you need to implement the `Loggable` interface, and then pass the object of the created class to the `setLogger(logger: Loggable)` method